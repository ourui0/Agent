[pytest]
markers =
    integration: tests that require external services, network access, Redis, API keys, or real model downloads
    slow: tests that are intentionally slower than the offline smoke suite
